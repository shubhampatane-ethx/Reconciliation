# Copy this file to .env and adjust values as needed.
# If DATABASE_URL is omitted entirely, the app falls back to the same
# local default shown below and keeps working on file storage alone.
DATABASE_URL=postgresql://consistency:consistency@localhost:5432/consistency
