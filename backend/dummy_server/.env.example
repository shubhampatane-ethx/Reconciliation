# NEW FILE — Phase 1 addition.
# Copy this file to backend/dummy_server/.env and adjust if needed.
# If DUMMY_SERVER_DATABASE_URL is omitted, it falls back to DATABASE_URL,
# then to the same local default the main backend already uses.
# DUMMY_SERVER_DATABASE_URL=postgresql://consistency:consistency@localhost:5432/consistency
# DUMMY_SERVER_HOST=0.0.0.0
#DUMMY_SERVER_PORT=9000


DUMMY_SERVER_DATABASE_URL=postgresql://postgres:Swamiom%401702%23@localhost:5432/Target_Data

DUMMY_SERVER_HOST=0.0.0.0
DUMMY_SERVER_PORT=9000