# Groq is the PRIMARY AI provider for the chat assistant; Ollama is the
# automatic fallback when Groq can't answer.
# Get a free API key at https://console.groq.com → API Keys. Never commit a
# real key to version control — keep it in this local .env file only.
GROQ_API_KEY=your-groq-api-key-here
